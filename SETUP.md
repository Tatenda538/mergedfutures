## (WINDOWS) Install Node 

we need to use `npm install`

https://nodejs.org/en/download/

## Install Opencode

Start > Command Prompt
```bash
npm i -g opencode-ai
```

## Supabase MCP

Authenticate with the Supabase MCP server:

```bash
opencode mcp auth supabase
```
